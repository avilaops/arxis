[package]
name = "avx-gpu-std"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Standard library for AVX-GPU (common GPU operations)"

[dependencies]
avx-gpu-core = { version = "0.1", path = "../avx-gpu-core" }
bytemuck.workspace = true
