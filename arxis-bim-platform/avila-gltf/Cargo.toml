[package]
name = "avila-gltf"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "glTF 2.0 / GLB exporter - complete spec implementation - 100% Rust"
keywords = ["gltf", "glb", "3d", "export", "graphics"]
categories = ["graphics", "rendering", "encoding"]

[dependencies]
avila-vec3d = { version = "0.1.0", path = "../avila-vec3d" }
avila-mesh = { version = "0.1.0", path = "../avila-mesh" }

serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
